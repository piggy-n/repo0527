{
  "name": "repo0527",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "deps:check": "pnpm outdated -r",
    "deps:update": "pnpm -r update"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11",
      "onFail": "download"
    }
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
